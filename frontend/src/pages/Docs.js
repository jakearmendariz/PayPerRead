export default function Docs() {
    return (
        <div>
            <h1>PayPerRead Docs</h1>
            PayPerRead uses iframes to be able read user's wallets, authenticate transactions and communicate with third party sites.
            <br></br>
            Additionally we have an open api for registering articles, which should be used upon creation or before selling of any content.
            
        </div>
    );
}